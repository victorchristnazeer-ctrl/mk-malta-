# ╔═══════════════════════════════════════════════════════════════════╗
# ║             TRADING BOT — ENVIRONMENT CONFIGURATION             ║
# ╚═══════════════════════════════════════════════════════════════════╝
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to git!

# ── Mode ─────────────────────────────────────────────────────────────
# Set to "true" for paper trading (no real money)
# Set to "false" or remove for live trading
PAPER_TRADING=true

# ── Exchange API Keys ────────────────────────────────────────────────
# Get your keys from: https://www.binance.com/en/my/settings/api-management
#
# For PAPER TRADING: you don't need API keys (uses simulated data)
# For LIVE TRADING:  you need API keys with "Enable Spot Trading" permission
#
# SECURITY TIPS:
#   - Use IP whitelist on Binance (restrict to your server IP)
#   - Never enable "Enable Withdrawals" permission
#   - Use a sub-account for the bot
API_KEY=
API_SECRET=

# ── Trading Settings ────────────────────────────────────────────────
# Trading pair (Binance format, no slash)
SYMBOL=BTCUSDT

# Candle interval: 1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d
TIMEFRAME=1h

# Strategy: ema_crossover, rsi, macd, bollinger, combined
STRATEGY=combined

# Starting balance (for paper trading / position sizing reference)
INITIAL_BALANCE=10000

# How often to check for new candles (milliseconds)
# 60000 = 1 minute (good for 1h candles)
# 15000 = 15 seconds (good for 1m-5m candles)
POLL_INTERVAL=60000

# ── Logging ──────────────────────────────────────────────────────────
# debug = everything | info = trades + status | warn = warnings only | error = errors only
LOG_LEVEL=info

# ── Advanced (optional) ─────────────────────────────────────────────
# Use Binance testnet instead of production
# TESTNET=true
#
# Exchange name (currently only binance supported)
# EXCHANGE=binance
